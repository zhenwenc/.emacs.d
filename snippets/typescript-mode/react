# -*- mode: snippet -*-
# name: React Bootstrap
# key: r
# condition: (and (zc-yas/react-buffer-p) (not (zc-yas/buffer-match-p "react")))
# --
import React from 'react';

${1:export interface ${2:`(file-name-base)`Props} {

}}

export function `(file-name-base)`(${3:props: $2}) {
  $0
}
